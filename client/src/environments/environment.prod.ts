/**
 * Production environment.
 * apiUrl and hubPath are typically overridden at build/deploy time.
 */
export const environment = {
  production: true,
  apiUrl: 'https://your-api.example.com',
  hubPath: '/hubs/chat',
  uiOnly: false
};
